# 대표 커밋 후보 (`09` §8)

> **제출물 ④ AI 활용 기술 문서 5.2(대표 커밋 인용)** 에서 해시를 인용해야 한다.
> **나중에 찾으면 못 찾는다** — 인용 가치가 생긴 그 자리에서 한 줄 적는다.
>
> 후보 유형: 최초 파이프라인 구축 / **AI 산출물을 되돌린 지점** / 방향 전환 /
> 밸런스 루프가 처음 자동으로 돈 시점 / CI 가드를 추가한 지점

---

## D+2 (2026-08-02)

| 해시 | 커밋 | 왜 인용 가치가 있나 |
|---|---|---|
| `e227e25` | `[H] chore: 저장소 개설 — 위생 설정과 커밋 규약을 코드보다 먼저 올린다` | **첫 커밋에 무엇을 넣을지가 그 자체로 판단**임을 보여준다. `.gitignore`/`.gitattributes`/규약은 나중에 넣으면 되돌리는 데 history rewrite 가 필요하고 그건 우리 규약이 금지한다 — **첫 커밋에만 할 수 있는 일**이었다 |
| `c72f1b6` | `[AI] feat(core): 결정론 기반 3종 + 회귀 테스트 22개` | ★ **AI 에게 코드가 아니라 제약을 지시한 사례.** 부동소수점 금지 / `System.Random` 금지 / 리스트 순회 순서 의존 금지 세 줄을 주고 구현을 위임했다. 전투 로직보다 먼저 짠 이유(검증 전제)가 메시지에 남아 있다 |
| `9091e87` | `[H] chore(unity,ci): 게이트 2·3 통과 + 규약을 CI 로 강제` | ★ **문서의 규칙을 기계가 강제하게 만든 지점.** 경계 가드·커밋 규약·게이트를 CI 로 옮겼다. husky 대신 CI 를 고른 이유(`.git/hooks` 는 심사자에게 안 보인다)도 함께 |
| `3bad987` | `[H] fix(core): nullable 문맥을 파일에 명시 — 두 컴파일러가 갈라져 있었다` | ★ **"설정했다"와 "적용됐다"가 다른 사례.** `Directory.Build.props` 를 Unity 가 읽지 않아 dotnet 쪽에만 nullable 이 켜져 있었다. **빌드를 실제로 돌리기 전까지 드러나지 않았다** |
| `29d0496` | `[H] fix(core): Newtonsoft 참조 누락 — Unity 만 보이고 dotnet 은 못 보던 상태였다` | ★ **위 사례의 반대 방향.** Unity 는 보고 dotnet 은 못 보던 상태라, JSON 코드를 짰으면 Unity 에서만 컴파일됐다. **두 컴파일러가 갈라지는 지점은 한 방향이 아니다** — 같은 날 양방향을 다 밟았다 |
| `af8c985` | `[H] docs: 상태 문서 3곳을 실제와 동기화 + D-49-b 확정` | **AI 가 스스로 낸 오류를 사람이 잡은 사례.** 이미 해소된 항목을 블로커로 잘못 안내했고, 원인이 매 세션 자동 로드되는 `CLAUDE.md` 의 낡은 문장이었다 |
| `443c265`<br>(`gh-pages`) | `[AI] chore(deploy): WebGL 빌드 배포 (게이트 1)` | 프로젝트 **최대 리스크를 D+2 에 닫은 지점.** 기계 커밋을 사람 커밋과 브랜치로 격리한 근거도 함께 |

### 이 날의 관통 주제 — **"했다"와 "된다"의 간극**

같은 형태의 실패가 **하루에 네 번** 나왔고 넷 다 **한 단계 더 실행해봤을 때만** 드러났다.

| | 설치·설정은 됐는데 | 실제로는 |
|---|---|---|
| Optuna | `pip install` 성공 | **ask-and-tell 을 돌려보기 전까지** 채택 근거가 증명 안 됨 |
| 게이트 3 | asmdef 4개 생성, 패키지 resolve | **스크립트 0개면 어셈블리가 아예 안 만들어진다** — 참조 여부를 모르는 채 초록불 |
| nullable | `Directory.Build.props` 에 설정 | **Unity 는 그 파일을 안 읽는다** |
| Newtonsoft | Unity 에서 잘 보임 | **dotnet 에서는 없었다** |

> ★ **기술 문서 4장(검증 기준)의 핵심 논지가 여기서 나온다.**
> *"AI 산출물을 어떻게 검증했는가"* 의 답이 **"선언을 읽지 않고 한 단계 더 실행했다"** 이고,
> 그 증거가 위 네 커밋의 `검증:` 줄에 각각 남아 있다.

---

## 갱신 규칙

- **그 자리에서 적는다.** 마감에 몰아서 고르면 이미 못 찾는다
- 해시는 **push 된 커밋**에서만 고른다 (rebase 로 로컬 해시가 바뀔 수 있다 — `09` §4)
- "잘 만든 커밋"이 아니라 **"판단이 드러난 커밋"** 을 고른다. 실패·되돌림이 오히려 값어치가 크다

---

## D+3 — 자율 진행 세션에서 나온 것

> **이날은 지시가 "체크리스트 보고 알아서"였다.** 사람이 구현 내용을 지정하지 않았고,
> AI 가 순서를 정해 진행했다. **그 상태에서 무엇이 드러났는가**가 이 표의 주제다.

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `a11daac` | `fix(core)`: 스스로 도는 상태이상과 각인 대상 | ★ **단위 테스트는 전부 통과하는데 스킬 3개가 아무 일도 안 하고 있었다.** 통합 테스트를 붙이자 드러났다 |
| `ff8e28a` | `feat(core)`: `skillPower` 의미를 데이터가 정했다 | ★ **AI 의 첫 판단이 데이터와 부딪혀 뒤집힌 기록.** 결론만 적으면 "그렇게 정했다"로 읽힌다 |
| `af7d577` | `fix(core)`: 표준 배치를 스펙대로 | 기술 문서에 *"표준 배치 기준으로 검증했다"* 를 쓸 예정인데 **그 표준이 문서와 달랐다** |
| `afce694` | `fix(unity)`: 스프라이트가 `Sprite` 로 임포트되지 않고 있었다 | ★ **팀원이 넣은 140장이 안 쓰이고 있었다.** 임포트 설정은 `.meta` 에 있어 코드에 안 보인다 |
| `17e450d` | `fix(deploy)`: 빌드 파일명 대소문자 — 배포본이 404 였다 | ★ **로컬에서 절대 재현되지 않는다.** 배포 후 URL 로 응답을 확인해서 잡았다 |
| `b19e65b` | `feat(sim)`: `M1`~`M7` — 첫 실측이 위반 4건 | 파이프라인이 처음으로 **게임을 판정**했다 |
| `cd0ccef` | `feat(sim)`: 만족도 함수 + 기하평균 `D` | ★ **목적함수가 곧 "좋은 밸런스"의 정의다.** `M4`·`M6` 을 뺀 이유가 본문에 있다 |
| `e62ee25` | `docs`: 구현이 만들어낸 미결 3건 등록 | ★ **"결정 단계가 끝났다"를 뒤집은 커밋.** 기획을 덜 한 게 아니라 재보기 전엔 물을 수 없던 질문이다 |

### ★ 이날의 패턴 — "넣었는데 안 쓰이고 있었다"

D+2 의 *"했다 ≠ 된다"* 보다 **한 층 안쪽**이다.

| 사례 | 상태 |
|---|---|
| 스킬 3개 | 컴파일 ✅ 단위 테스트 ✅ **전투 루프가 안 부름** |
| 스프라이트 140장 | 파일 ✅ 커밋 ✅ **임포트 타입이 `Sprite` 가 아님** |
| 아이콘 30개 | 데이터 ✅ 파일 ✅ **색인이 명명 규칙 하나만 앎** |
| 배포 로더 | 빌드 ✅ 푸시 ✅ **대소문자가 달라 404** |

> **넷 다 "확인했다"고 말할 근거가 있는 상태였다.** 파일도 코드도 멀쩡히 있고 테스트도 통과한다.
> **쓰는 쪽이 한 번 돌려보기 전까지 아무도 모른다.**
> → `18` 4장에 이 형태를 그대로 쓴다. 검증 기준이 *"선언을 읽지 않는다"* 에서
> **"쓰는 쪽에서 한 번 돌려본다"** 로 한 단계 더 내려간다.

## D+3 2회차 (2026-08-03) — 사람과 기계의 경계가 실측으로 그어졌다

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `76f91af` | `[H] data`: `C2-B` 파동 — 광역을 3초 주기 안으로 | ★★ **최적화기가 할 수 없는 수정이었다.** 값이 아니라 구조라서. 한 줄이 `M1` 을 48% → 33.4% 로 옮겼다 |
| `dd0799f` | `[H] feat(sim)`: `M4` 재정의 + `M6` 제외 | ★★ **AI 가 구현해둔 해석을 사람이 버린 기록.** 그 판단이 옳았다는 게 같은 날 실측으로 확인됐다 |
| `9097a60` | `[AI] feat(core)`: 지속 보호막 2종 | ★ **`M1`~`M7` 로는 영영 못 잡는 버그.** 전투가 3.4초라 10초 주기가 안 온다 — 지표가 침묵하는 영역 |
| `c69ac75` | `[AI] feat(core)`: 중첩 `aoe` 실행 경로 | 사람의 데이터 결정을 받기 위해 **엔진에 자리를 먼저 만든** 커밋. 다음 커밋과 짝이다 |
| `ac557f8` | `[AI] feat(core)`: `conditionalBoost` | **`sim` 지표로는 검증되지 않는 코드**임을 커밋 본문에 명시했다 — 봇이 아이템을 안 산다 |

### ★ 이날의 패턴 — "지표가 침묵하는 영역"

D+2 *"했다 ≠ 된다"* → D+3 *"넣었는데 안 쓰이고 있었다"* → **D+3 2회차 *"틀린 채로 작동한다"***

| 사례 | 왜 지표가 못 잡나 |
|---|---|
| 보호막 재충전 | 전투가 3.4초라 **10초 주기가 오지 않는다.** `D` 가 0.0013 움직였다 |
| `conditionalBoost` | **봇이 아이템을 안 산다**(`08` §6.1). `M1`~`M7` 에 아예 안 나타난다 |
| `GrantShield` 상한 | 겹치는 조합이 **한 번도 발생한 적이 없었다** — `C4-P2` 가 안 걸리고 있어서 |
| `sim` 지표 계층 | **테스트가 0 이었다.** 틀려도 숫자는 계속 나온다 |

> **넷 다 "지표가 초록이다"가 근거가 되지 않는 자리다.**
> → `18` 4장: 검증 기준이 *"쓰는 쪽에서 한 번 돌려본다"* 에서
> **"지표가 그걸 볼 수 있는 조건인지 먼저 확인한다"** 로 한 단계 더 내려간다.

### D+3 2회차 — `P4` 도구가 다 섰다

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `0e915e3` | `[AI] feat(sim)`: `params.json` 파라미터 덮어쓰기 | ★★ **`D-49-a` 의 전제가 실은 성립하지 않고 있었다.** *"외부 루프를 Python 에 둔다"* 고 정해놓고 **시도할 값을 넘길 수단이 없었다** |
| `352af77` | `[AI] feat(tune)`: Morris + Optuna | ★★ **축차 반감법 설계가 실측에 부딪힌 기록.** 표본을 줄이면 목적함수가 0 이 된다 — 교과서에 없는 실패다 |
| `b6f624e` | `[H] fix(tune)`: git 출력 인코딩 | ★ **provenance 가 조용히 거짓말을 하고 있었다.** 예외를 잡느라 `workingTreeDirty` 가 항상 `true` 였다 |

> ★ **셋 다 "설계는 맞았는데 그 설계가 실제로 서 있지 않았다" 는 형태다.**
> `D-49-a`(2026-08-01)는 옳은 결정이었고 지금도 옳다 — 다만 **그 결정이 코드로 닫힌 건 D+3 2회차 다.**
> 결정 문서와 코드 사이의 간격은 **둘 다 초록일 때 가장 안 보인다.**

### D+3 2회차 마감 — 밸런스 루프가 한 바퀴 돌았다

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `fe8a191` | `[BAL] data`: 캐릭터 스탯 9개 + 액티브 가격 | ★★★ **파이프라인이 닫혔다는 유일한 증거.** 기계가 제안하고 사람이 세트째 판정했다 — `D` 0.807 → **0.923** |
| `a4a10ab` | `[H] feat(sim)`: `M5` 재정의 (`D-74`) | ★★★ **AI 가 자기 등록 근거를 스스로 뒤집은 커밋.** 전투만 재면서 전체 런 목표를 대고 있었다 |
| `aa7fabe` | `[H] feat(core)`: 회피 이벤트 (v1.1) | ★ **동결을 깨지 않고 늘렸다** — 맨 뒤에 붙이면 기존 번호가 안 밀린다는 게 동결의 실질이다 |

### ★★ 이날의 결론 — 지표는 자기가 틀렸다고 말해주지 않는다

D+2 *"했다 ≠ 된다"* → D+3 *"넣었는데 안 쓰이고 있었다"* → D+3 2회차 *"지표가 침묵한다"*
→ **D+3 2회차 마감 *"지표가 틀린 것을 재고 있어도 숫자는 나온다"***

| 사례 | 숫자는 |
|---|---|
| `M5` 가 전투만 재는데 목표는 전체 런 | `d`=0.153 — **정상적인 "나쁨"으로 보였다** |
| 보호막 재충전 미구현 | `D` 가 0.0013 움직였다 |
| `conditionalBoost` 미구현 | 지표에 **아예 안 나타났다** |
| 데이터에 박힌 테스트 5건 | **첫 `[BAL]` 전까지 전부 초록이었다** |

> ★ **`M5` 는 특히 위험했다.** 낮은 점수는 *"게임이 짧다"* 로도 *"비교 대상이 틀렸다"* 로도 읽힌다.
> 앞의 해석을 믿고 **적 스탯 28축을 최적화기에 넘길 뻔했고**, 그랬으면
> **이미 목표 근처인 전투를 6.5배 늘려** 영상 제약(`05` §1.8)까지 깨뜨렸을 것이다.
>
> → `18` §4.3: 검증 기준의 마지막 층은 *"지표가 통과했는가"* 가 아니라
> **"이 지표가 내가 묻는 질문에 답하고 있는가"** 다.

### D+5 — 합격한 지표 안에 숨어 있던 버그

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `353e139` | `[AI] fix(core)`: 시체가 보드 칸을 영구 점유 | ★★ **사람이 원인 지점까지 잡아왔고, 그 사람의 후보 수정을 AI 가 되짚어 버그를 찾았다.** `board.Remove(u.At)` 는 매 틱 다시 돌아 그 칸에 들어온 산 유닛을 지운다 — 지시가 아니라 **가드를 `Board` 안으로 옮겨** 잊을 수 없게 했다 |
| `7e3c166` | `[AI] test(core,ci)`: 게이트 4 — 전투 불변식 | ★★★ **"버그를 고쳤다"가 아니라 "왜 아무도 못 잡았나"에 답한 커밋.** 테스트를 더 쓰는 대신 방향을 뒤집었다 — *무엇이 틀렸을까* 가 아니라 *무엇이 항상 참인가*. 실증까지 붙였다: 수정만 끄고 검사만 켜니 **21,600런을 통과하던 버그가 틱 1에서 터진다** |
| `a1b6239` | `[AI] chore(tools)`: 미호출 멤버 감사 | ★★ **게이트로 만들지 *않기로* 한 판단이 본문에 남은 커밋.** 오탐률 70%를 초록/빨강으로 쓰면 게이트가 하나 죽는다. 그리고 이 스크립트 자체가 첫 판에 조용히 망가져 "0건"을 냈다 — **검사 도구는 스스로를 검사해야 한다** |

### ★★★ 이날의 결론 — 임계값은 "정상"이 아니라 "봐주는 선"이다

이 버그의 유일한 흔적은 `M7`(타임아웃) **1.67%** 였고 목표가 `<5%` 라 **합격**이었다.
그런데 서든데스는 반드시 누군가를 죽이므로 **구조상 0에 가까워야 하는 지표**다.
수정 후 정확히 **0.00%**.

| 날짜 | 지표가 |
|---|---|
| D+3 2회차 | 틀린 것을 재고 있어도 **숫자는 나온다** (`M5`) |
| **D+5** | **맞는 것을 재고 통과했는데도 버그가 그 안에 있었다** (`M7`) |

> `18` §4.3 에 한 층 더: *"이 지표가 내가 묻는 질문에 답하고 있는가"* 다음은
> **"구조상 0이어야 하는 값인데 0이 아닌가"** 다. 그런 지표에는 임계값을 걸면 안 된다.

---

## D+6~D+7 — 겹쳐서 서로를 무력화한 것들

이 구간의 인용 가치는 한 종류로 모인다 — **각각은 맞는 변경 둘이 겹치면서 한쪽을 죽인 경우.**
단일 버그보다 찾기 어렵고, 기술 문서 6장(한계와 대응)에 그대로 쓸 수 있다.

| 커밋 | 무엇 | 왜 인용 가치가 있나 |
|---|---|---|
| `b98448d` | `[AI] fix(unity)`: 팝업 버튼이 클릭을 뺏김 | ★★ **두 코드 캔버스가 `sortingOrder` 를 각자 8000 으로 박아** 동률이 됐다. 동률이면 어느 쪽이 클릭을 먼저 받는지 **화면 계층과 무관하게** 정해진다 — 값을 고치지 않고 `UITheme.Layer` 로 **발급처를 하나로** 모았다 |
| `5cfb5bd` | `Merge`: 팀원 11커밋 | ★★★ **git 이 서로 무관한 GameObject 를 짝지었다** — main 의 `TitleLabel` 과 팀원의 `SkillCard1` 이 한 충돌 덩어리로 엮여 6개 전부 읽을 수 없었다. 텍스트 병합을 버리고 `UnityYAMLMerge` 로 다시 병합. **검증 3종**(고아 fileID·중복 anchor·직렬화 필드 집합)을 세워 Unity 를 켜기 전에 판정했다 |
| `e933b3a` | `[AI] fix(unity)`: 툴팁 주인 개념 | ★★★ **주인 없는 전역 상태를 둘이 매 프레임 덮어썼다.** `StageIntro` 는 `GamePlay` 위에 뜨는 팝업이라 뒤에서 보드가 계속 유닛을 짚었고, 그 결과 ⑴ 적 툴팁이 같은 프레임에 지워지고 ⑵ **화면에 있지도 않은 용병 스펙**이 떴다. `Show(owner)`/`Hide(owner)` 로 **띄운 쪽만 끌 수 있게** 했다 |
| `6ecdb4e` | `[AI] fix(unity)`: 빈 전장이 기본 전장을 가림 | ★★ 폴백 사슬(구체적→일반)을 먼저 짜고, 나중에 *"빈 프리팹을 미리 깔면 칠할 자리가 보인다"* 를 더했더니 **그 빈 파일이 사슬을 끊었다.** 두 기능 다 맞는데 겹쳐서 기본 전장이 후보에서 빠졌다 |

### ★★★ 이 구간의 결론 — "연출이 없다"가 아니라 "가려져 있다"

같은 모양이 세 번 나왔다.

| 증상 | 진짜 원인 |
|---|---|
| 도트 애니메이션이 **머리만 움직인다** | 체력 막대가 어깨선 아래를 덮고 있었다 (`7cd5a7c`) |
| 이기자마자 팝업이라 **끝난 실감이 없다** | 마지막 적이 죽은 **그 프레임에 판이 지워졌다** (`07e75f7`) |
| 적 초상화에 올려도 **정보가 안 뜬다** | 다른 화면이 매 프레임 툴팁을 지우고 있었다 (`e933b3a`) |

셋 다 "더 넣자"로 갔으면 화려하게 만들고도 여전히 안 보였을 것이다.

> **안 보이는 것의 원인을 "부족"에서 찾기 전에 "가려짐"을 먼저 볼 것.**
> 더하는 쪽이 손에 익어 먼저 떠오르는데, 값이 싼 쪽은 대개 치우는 쪽이다.

### ⚠️ 인용할 때 같이 적어야 하는 것 — 이날 규약을 어겼다

`[AI]` 커밋 6건(`7cd5a7c` `ac3f0ae` `9b7adb6` `d10c92e` `e2a30fb` `07e75f7`)에
`09` §1.3 이 요구하는 **`의도:` 라벨이 빠져 CI 가 실패했다**(런 `31162836351`).
본문에 "왜"는 길게 썼는데 **기계가 보는 라벨만 없었다.**

되돌리지 않았다 — `09` §4 의 force push 금지가 *"시행착오도 남긴다"* 를 근거로 삼는데,
**규약 위반을 규약 위반으로 지우면 앞뒤가 안 맞는다.**

> 기술 문서 5.2 에 쓸 때 이 건을 빼면 안 된다. **게이트가 실제로 작동한 증거**이고,
> "사람이 지켜야 하는 규칙보다 어길 수 없는 규칙이 낫다"(`fast.yml` 머리말)의 실증이다.
