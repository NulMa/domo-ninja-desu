# 작업환경 인수인계 (PC 이전용)

작성: 2026-08-01 (D+1)
**마감: 2026-08-10 23:59:59 / 제출 목표 08-09 (D+9) — 남은 기간 9일**

> 다른 PC에서 이어서 작업하기 위한 문서. **§1부터 순서대로 따라가면 된다.**

---

## 1. 옮겨야 할 것

### 1.1 프로젝트 폴더 통째로

```
C:\Users\NullMa\NAN_pre\
```

| 경로 | 내용 | 크기 | 비고 |
|---|---|---|---|
| `CLAUDE.md` | 프로젝트 전역 규칙 + 문서 인덱스 | 소 | **세션마다 자동 로드됨. 필수** |
| `docs/` | 00~16번 문서 17개 | 소 | 프로젝트의 실체 |
| `data/` | `_schema/` + JSON 4개 | 소 | 게임 데이터 계약 |
| `temp/` | A 트랙 인수인계 3종 | 소 | 팀원 주고받은 것 |
| `Hackerton/` | 팀원 기획 뼈대 + 역기획 3건 | 소 | ⚠️ **제출 저장소에는 넣지 않는다** (`D-26`) |
| `Ninja Adventure - Asset Pack/` | 에셋 팩 (CC0) | **109 MB** | §1.3 참조 |

### 1.2 메모리 — **`_memory_backup/` 에 복사해뒀다. 폴더만 옮기면 된다** ✅

원본 위치는 프로젝트 밖이다.

```
C:\Users\NullMa\.claude\projects\C--Users-NullMa-NAN-pre\memory\
```

이 경로는 **프로젝트 경로에서 파생**되므로 PC마다 폴더명이 달라질 수 있다.
그래서 사본을 **`NAN_pre/_memory_backup/`** 에 넣어뒀다 → 프로젝트 폴더와 함께 자동으로 따라온다.

**새 PC에서 할 일:** Claude Code 세션에서 한 줄이면 된다.

> `_memory_backup/` 안의 파일들을 이 PC의 프로젝트 메모리 위치에 저장해줘

경로를 직접 찾을 필요가 없다. 상세는 `_memory_backup/README.md`.

⚠️ 이전 후에는 **원본과 사본이 갈라지지 않게** 한쪽만 유지하고, `.gitignore` 에 `_memory_backup/` 을 넣는다.

### 1.3 ⚠️ 에셋 팩 109MB — 통째로 커밋하지 말 것

저장소는 public이고 심사자가 클론한다. 109MB는 클론을 느리게 하고 WebGL 빌드에도 불필요하다.

> **권고: `assets/` 에 실제 사용분만 추린다.**
> 캐릭터 6종 + 몬스터·보스 8~10종 + UI 테마 + 스킬 아이콘 12~15개 + FX 일부 + BGM 3~4곡 + 효과음 10여 개
> → 수 MB 수준으로 떨어진다. 원본 팩 출처는 `14_에셋_라이선스.md` 에 이미 기재돼 있으므로 재현성에도 문제없다.

### 1.4 참고용 (프로젝트 외부, 선택)

```
C:\Users\NullMa\Desktop\Dev\00_ProjectSettings\
  unity-project-kickstart.md      ← D+1 저장소 개설 시퀀스로 활용
  context-management-harness.md
  agent-skill-guide.md
```

---

## 2. 새 PC에서 설치할 것

| # | 항목 | 용도 | 비고 |
|---|---|---|---|
| 1 | **Git** + 계정 설정 | — | ⚠️ **`user.name`/`user.email` 을 팀원과 맞춘 값으로.** 커밋 author가 ⑤ 롤 기술서와 대조된다 (`D-03`) |
| 2 | **Unity 6.x** | 게임 클라이언트 | WebGL 빌드 모듈 포함 설치 |
| 3 | **.NET SDK 8+** | `core` / `sim` 빌드·테스트 | 시뮬은 Unity 없이 `dotnet` 으로 돈다 |
| 4 | **Unity-MCP** (IvanMurzak, MIT) | AI가 Unity 콘솔·씬 직접 접근 | `13_외부도구_조사.md` §4.5.1 |
| 5 | Newtonsoft.Json for Unity | JSON 로드 | Unity 패키지 매니저 |
| 6 | (선택) Game-UI-Reference-Cli | UI 레퍼런스 인덱싱 | Python 3.10+, 로컬 인덱싱은 의존성 0 |

---

## 3. 현재 상태 — 한 화면 요약

### 3.1 확정된 것

| 항목 | 값 |
|---|---|
| 참가 형태 | 2인 팀 → 제출물 5종 전부 필요 |
| 제출 형식 | 웹 빌드 + GitHub Pages |
| 장르·구조 | 라운드제 PvE 오토배틀러 / 8라운드 · 생명 3 / **로스터 6명 중 3명 출전** |
| 핵심 메커닉 | **캐릭터당 스킬 2택, 구매 순간 나머지 영구 소멸** |
| 코어 구조 | 전투 로직을 **Unity 비의존 순수 C#** 으로 분리 + **결정론 강제** |
| AI 활용 축 | **탐색(최적화 알고리즘) + 디렉팅(사람·LLM)** 2층 밸런스 루프 |
| 에셋 | Ninja Adventure (CC0 1.0) 단일 소스 |
| 커밋 규약 | `[AI]`/`[H]`/`[BAL]`/`[REV]` 태그 + `의도:` 필수 |

### 3.2 진행률

| 트랙 | 상태 |
|---|---|
| **B (콘텐츠·데이터)** | ✅ **완료.** `/data` 4개 + 스키마 계약 작성됨 |
| **A (전투 엔진)** | ⚠️ 팀원 확정본 도착. **6건이 우리 설계와 갈림 → `15_A트랙_대조.md`** |
| **저장소** | ❌ 미개설. `D-01`(팀대표자) 대기 |
| **구현** | ❌ 미착수 |

### 3.3 ⚠️ 지금 막혀 있는 것

**A 트랙 확정본이 우리 데이터 상당수를 무효화한다.** 팀원 회신 전까지 `/data` 재작업을 시작할 수 없다.

| 무효화 | 사유 |
|---|---|
| `characters.json` 의 `placementRule`, `frontlineAffinity` | `A-3` 전열/후열 폐기 |
| `skills.json` 의 `C3-B 표창`, `C4-A 저격` | 후열 개념 소멸 |
| `encounters.json` 의 `front`/`back` 배치 | 좌표 기반으로 변경 필요 |
| 스키마 §2 `range` 정의 | 정수 제곱거리 임계값으로 재정의 |

**팀원에게 던진 질문 6건** (§4 of `15_A트랙_대조.md`) — Q1~Q3 회신되면 재작업 착수 가능.

---

## 4. 새 PC에서 이어서 할 일 (우선순위)

### 4.1 팀원 회신을 기다리는 동안 할 수 있는 것

- [ ] **`D-05`** AI 지시 아카이브 방식 확정
- [ ] **`D-06`** AI 활용 기술 문서 목차 확정 ← **`03번` 6장 기준 원래 1순위인데 계속 밀려 있다**
- [ ] **`D-01`/`D-02`/`D-03`** 팀대표자·저장소·git config → 정해지면 **저장소 개설 후 `docs/`+`data/` 첫 커밋**
- [ ] 에셋 큐레이션 (§1.3) — 사용할 스프라이트 6+10종 추리기
- [ ] `12_작업분담.md` §1 에 `unity-project-kickstart.md` 세팅 시퀀스 반영

### 4.2 회신 오면 즉시

- [ ] `/data` 4개 파일 + 스키마 재작업 (`15_A트랙_대조.md` §5 목록)
- [ ] `08_게임_스펙.md` 「전투 시스템」 절 신설 → **v0.5**
- [ ] **★ 빌드 공간 방어** — 시뮬의 배치를 고정 규칙 또는 프리셋 3~5종으로 제한.
      `M4` 를 "추정이 아닌 정확값"으로 유지하는 게 `D-30` 채택의 근거였다 (`15번` §3)

### 4.3 그다음 (D+2 게이트 — 협상 불가)

| 게이트 | 내용 |
|---|---|
| 1 | 빈 씬 WebGL 빌드 → GitHub Pages → **시크릿 브라우저 접속 성공** ← 최대 리스크 |
| 2 | `sim` 콘솔 앱이 CI에서 `dotnet` 으로 실행되어 리포트 산출 |
| 3 | Unity가 `core` 를 로컬 UPM 패키지로 참조해 컴파일 성공 |

---

## 5. 문서 읽는 순서 (새 세션에서)

1. `CLAUDE.md` — 자동 로드됨
2. **`docs/00_진행로그.md`** — 날짜별 경과·판단 번복 기록
3. `docs/05_결정사항.md` — 확정된 방향
4. `docs/06_결정_대기목록.md` — 남은 결정 (D-00~D-44)
5. `docs/08_게임_스펙.md` v0.4a — 게임 설계
6. **`docs/15_A트랙_대조.md`** — 현재 최대 이슈
7. `data/_schema/README.md` — 구현 착수 시 계약

---

## 6. 잊으면 안 되는 것

- [ ] **`D-00` 참가 신청 폼 필드 확인** — 신청 마감 = 제출 마감(08-10). **폼이 곧 제출 창구일 가능성이 높다.** 비용 10분, 안 하면 마지막 날 사고
- [ ] **`D-24` 대회 홈페이지 공지 확인** — 약관 제11조상 변경은 공지로 갈음되며 **개별 통지가 없다**
- [ ] 커밋 시작 후 **squash / force push 금지**
- [ ] 에셋 쓰는 즉시 `14_에셋_라이선스.md` §2에 기록
