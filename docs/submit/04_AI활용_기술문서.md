# 제출물 ④ AI 활용 기술 문서

**담당: 사용자 + Claude** · 구성 근거: `../18_기술문서_구성.md`
상태: **골격** — D+7~9에 채운다

> **요강이 요구하는 건 두 줄뿐이다.**
> ① AI 활용에 대한 기술적 설명(구조 설명, AI 대상 주요 프롬프트 및 지시 사항) ② 외부 에셋 / 오픈소스 출처
> **아래 8장 중 2·3·8장이 그것이고, 나머지 5개 장은 자발적 추가다.** 그 자발분이 순위를 가른다.

---

## 0. 요강 대응표 🔴 — **첫 장에 둔다**

> ⚠️ **이 표가 이 문서에서 가장 실용적인 부분이다.**
> 요구하지 않은 내용이 앞에 길게 깔리면 심사자가 **필수 항목을 못 찾는다.**
> 조판 문제가 아니라 **읽히게 만드는 설계**다.

| 요강 요구사항 | 이 문서의 위치 |
|---|---|
| AI 활용 기술적 설명 — **구조 설명** | **2장** |
| AI 활용 기술적 설명 — **주요 프롬프트 및 지시 사항** | **3장** |
| **외부 에셋 / 오픈소스 출처** | **8장** |
| *(유의사항)* 외부 에셋 라이선스 명시 | **8장** |
| *(유의사항)* 사용 AI 도구와 활용 내역 | **8장** + 1장 |
| — 이하 자발적 추가 — | 1 · 4 · 5 · 6 · 7장 |

---

## 1. 요약 — AI는 무엇을 했고 사람은 무엇을 했는가

### 1.1 역할 분담

`TBD` — 소스: `18` §1, `20_기여기록.md` §0

```
사람      : 무엇이 좋은 밸런스인가를 정의 (목적함수 · M4a/M4b)
          : 무엇을 AI에게 맡기지 않을 것인가
          : AI 산출물의 채택·기각
AI(Claude): 문서·코드 생성, 스펙 대조, 설계 감사
알고리즘   : 파라미터 탐색 (Optuna/SALib) — LLM이 아니다
```

### 1.2 한 줄 요약

`TBD` — 초안: *"AI에게 코드를 짜게 한 프로젝트가 아니라, **무엇을 검증할지 정의하고 그 검증을 자동화한** 프로젝트다."*

### 1.3 사용한 AI 도구

`TBD` — Claude (Claude Code). 소스: `14` §5

---

## 2. AI 개발 파이프라인 구조 🔴 요강 필수 — "구조 설명"

### 2.1 전체 워크플로

`TBD` — 소스: `18` §2.1, `05` §1.5, `13` §1.3

> ★ 강조: **이 루프에 런타임 LLM 호출이 0**이다. 개발 중 LLM도 탐색이 아니라 **정의와 판단**에만 쓴다.

### 2.2 각 단계에서 사용한 도구와 그 이유

`TBD` — 소스: `13` 전체, `17` §8

> ★ **이 절의 핵심은 채택 목록이 아니라 기각 목록이다.**
> BMAD/Spec Kit · FixedMathSharp · OpenSpiel · CMAES.NET · 만족도 함수 라이브러리 · LLM을 최적화기로.
>
> ★ **반드시 쓸 문장**: *"표준 수치 도구는 가져다 썼고, **무엇을 최적화할지는 우리가 정의했다.**"*
> 이 문장이 없으면 도구 목록만 남아 "활용"으로 읽힌다.

### 2.3 단계 간 산출물 연결

`TBD` — 소스: `data/_schema/README.md`

> ★ `balance.json` 의 `provenance`(시뮬 커밋 해시·데이터 해시·시드)를 인용한다.
> **밸런스 리포트는 숫자보다 출처가 중요하다.**

---

## 3. AI 대상 주요 프롬프트 및 지시 사항 🔴 요강 필수

### 3.1 프로젝트 전역 규칙

`TBD` — `CLAUDE.md` 원문 인용. 소스: `18` §3.1

> ★ 강조: **커밋 작성 원칙을 전역 규칙에 박아뒀다** — *"커밋은 사후 산출물이 아니라 설계 대상이다"*

### 3.2 단계별 핵심 지시문 — 원문 인용

`TBD` — 소스: `docs/ai-log/`

> ⚠️ **D+0~D+1은 소급 재구성이고, 그 사실을 명시한다.** 원문이 없는 걸 원문인 척하지 않는다.
>
> 인용 후보 (확보됨)
> - *"최적화 관점도 생각해서 **어떤 도메인 지식이나 방법**을 쓰면 좋을지 조사"* → 도구가 아니라 도메인 지식을 요구 → `M4` 재정의
> - *"결정해줄 다른 사항이나 **기획의 공백**은 없는지 더 찾아봐"* → 설계 감사 8건
> - *"**팀원이 정할 부분**이지 않나, 일단 넘어가"* → 역할 경계 유지

### 3.3 실패한 지시와 그 수정 과정 ★ 차별화 지점

`TBD` — 소스: `18` §3.3, `00_진행로그.md`

| # | 실패 | 수정 |
|---|---|---|
| 1 | BM을 "본선 카드로 접어둔다" | 프레이밍 자체가 성립 안 함 → **완전 배제** |
| 2 | 전투 설계를 구현 비용 기준으로 추천 | **6건 뒤집힘.** "게임으로 성립하는가"가 맞았다 |
| 3 | "베이지안 최적화는 C# 라이브러리가 없어 불채택" | 관찰은 맞고 **전제가 틀렸다** |
| 4 | 오픈소스 평가에 star 수를 기계적으로 적용 | **블래스트 반경**을 선행 질문으로 추가 |
| 5 | 액티브/패시브 `type` 필드 추가 제안 | 이미 있던 걸로 흡수. **스키마 변경 0** |
| 6 | 팀원 `[TBD]` 문서를 최대 규모로 해석해 동결 | **메타 프로그레션이 처음부터 있었다** |

> ★ 6건 모두 **문서에 뒤집힘이 그대로 남아 있다.** 지우지 않은 게 증거다.

---

## 4. 사람이 개입한 지점과 그 판단 기준

### 4.1 AI에게 맡기지 않기로 한 영역

`TBD` — 소스: `18` §4.1

### 4.2 AI 산출물을 되돌린 사례 ★

`TBD` — **최고의 재료는 `06` §H 종결표다.** 추천 14건 중 **6건이 뒤집혔고 표로 나란히 남아 있다.**

> *"AI 추천은 전부 구현 비용·결정론 유지 비용·시뮬 속도 쪽으로 기울어 있었고,
> 사람 판단은 게임으로서 성립하는가 쪽이었다. 후자가 맞았다.
> **2×2 고정 배치는 오토배틀러가 아니라 숫자 비교다.**"*

### 4.3 검증 기준

`TBD` — 소스: `18` §4.3

| 층 | 기준 |
|---|---|
| 데이터 | 로드 검증 20개. **실패하면 즉시 에러** |
| 결정론 | 같은 시드 2회 → **결과 해시 동일** (CI) |
| 경계 | `core` 에 `UnityEngine` 금지 — **asmdef `noEngineReferences` + CI grep 두 겹** |
| 커밋 | 메시지 규약 정규식 검증 (CI) |
| 밸런스 | `M1`~`M7` → 만족도 기하평균 `D`. **하나라도 0이면 전체 0** |

> ★ 실례: `A-5` 디버프 "매 틱" 표기를 **산술로 검증**해 5초 전멸임을 보이고 되물은 건.
> *"확인 필요"로 넘기지 않고 숫자를 냈다.*

---

## 5. 커밋 히스토리로 본 개발 과정

### 5.1 커밋 전략 설계 원칙

`TBD` — 소스: `09_커밋_규약.md`

> ★ **왜 이렇게 설계했는가**: 요강이 커밋 기록을 요구한 건 **결과물만 볼 거면 요구할 이유가 없기 때문**이다.

### 5.2 대표 커밋 인용 (해시 + 그 시점의 의사결정)

`TBD` — 소스: `docs/commit-highlights.md`

### 5.3 히스토리에서 읽히는 개발 리듬

`TBD` — **밸런스 이터레이션 1회 = 커밋 1개**라 리듬이 자연 발생한다

---

## 6. 비용·성능 관리

### 6.1 API 호출 설계

`TBD` — **런타임 LLM 호출 0.** 탐색은 Optuna가 무료로 돈다

### 6.2 런타임 AI의 지연·실패 처리

`TBD` — **해당 없음. 그리고 그게 결론이다.**
*"안 썼다"* 가 아니라 **"쓰지 않기로 판단했다"** 로 서술한다

---

## 7. 재현성 및 확장성 ★ 본선 진출 어필

### 7.1 파이프라인 이식성

`TBD` — **`17` 이 통째로 이 절이다.** 6개 방법이 전부 **장르 비의존**이다

### 7.2 48시간 본선 적용 시나리오

`TBD` — 🔴 **D+8까지 반드시 쓴다.** 아직 문장이 없는 유일한 절

> 방향: 48시간에서 가장 비싼 건 **밸런스 감 잡기**인데 이 파이프라인은 그걸 **CI에 넘긴다.**
> 주제 공개 → **지표 정의(사람, 2시간)** → 코어 구현 → 시뮬 → 나머지는 자동.

---

## 8. 외부 에셋 / 오픈소스 출처 및 라이선스 🔴 요강 필수

`TBD` — **`14_에셋_라이선스.md` 가 이미 제출 가능한 형태다. 그대로 옮긴다.**

- 에셋: Ninja Adventure (CC0 1.0) — ①~④ 4개 항목 확인 기록 포함
- 오픈소스: 런타임 2 / 빌드·CI 2 / 개발 도구 5
- 참고 문헌 R1~R6 — **코드 차용 없이 설계 근거로만 인용**
- ⚠️ **R2(α-Rank)는 "썼다"고 쓰면 사실과 다르다.** PvE라 적용 조건이 안 맞는다는 것까지 함께 쓴다

---

## 작성 시 확인

- [ ] **0장 요강 대응표**가 맨 앞에 있는가
- [ ] 2·3·8장(요강 필수)이 채워졌는가
- [ ] 3.2에 **실제 지시문 원문**이 인용됐는가 (소급분은 소급이라 명시)
- [ ] 3.3에 **실패 사례**가 있는가 — 성공만 나열하면 운으로 읽힌다
- [ ] 커밋 해시가 인용됐는가 (문서 ↔ 저장소 상호 검증)
- [ ] 8장에 **에셋 라이선스**와 **AI 도구 활용 내역**이 둘 다 있는가 (유의사항)
- [ ] 7.2가 채워졌는가 — **본선 심사에 대한 직접적 답변**
- [ ] `TBD` 가 0건인가
