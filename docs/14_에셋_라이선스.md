# 외부 에셋 · 오픈소스 출처 및 라이선스

최종 갱신: 2026-08-06 (D+6) — §2 상점 스킬/아이템 아이콘 + 재화 아이콘(메타·런) 사용 내역
추가 갱신: 2026-08-06 (D+6) — §1 **A3 Neo둥근모** 추가 · §2 UI 테마 스프라이트 사용 내역
추가 갱신: 2026-08-05 (D+5) — §1 **A2 Pretendard 폰트** 추가

> **제출물 ④ AI 활용 기술 문서의 필수 항목이다** (요강: "외부 에셋(이미지·사운드 등) 사용 시 출처와 라이선스를 AI 활용 기술 문서에 반드시 명시").
> 약관 **제4조 3항**(라이선스 준수 의무) · **제9조 2항**(적법성 책임은 전적으로 참가자)에 걸리는 항목이므로,
> **쓰는 즉시 여기 기록한다.** 나중에 역추적하면 반드시 빠진다.

---

## 1. 그래픽 · 오디오 에셋

| # | 에셋 | 제작자 | 라이선스 | 출처 | 사용 범위 |
|---|---|---|---|---|---|
| A1 | **Ninja Adventure — Asset Pack** | pixel-boy (Pixel-Boy & AAA) | **CC0 1.0 Universal** | [itch.io](https://pixel-boy.itch.io/ninja-adventure-asset-pack) | 캐릭터·적·보스 스프라이트, 페이스셋, UI, 스킬 아이콘, FX, 효과음, BGM |
| A2 | **Pretendard** (Regular) | Kil Hyung-jin (orioncactus) 외 | **SIL Open Font License 1.1** | [GitHub](https://github.com/orioncactus/pretendard) | ~~UI 전체 텍스트~~ → **`A3` 의 폴백 전용**(2026-08-06). `neodgm` 에 없는 글자만 그린다 |
| A3 | **Neo둥근모** (`neodgm.ttf` v1.601) | Eunbin Jeong (Dalgona.) | **SIL Open Font License 1.1** | [GitHub](https://github.com/neodgm/neodgm) | **Unity UI 전체 텍스트**(TextMeshPro SDF). 픽셀 에셋과 맞는 비트맵 계열 한글 폰트 |

### A1 라이선스 확인 기록

- 팩 내 `LICENSE.txt` 에 **CC0 1.0 Universal 전문**이 동봉되어 있음 (2026-07-31 원문 확인)
- 배포 페이지 문구: *"You can use any and all of the assets found in this package in your own games, even commercial ones. **Attribution is not required but appreciated.**"*

| 확인 항목 | 결과 |
|---|---|
| ① CC0 여부 | ✅ CC0 1.0 Universal |
| ② 상업적 이용 | ✅ 가능 |
| ③ **공개 저장소 포함·재배포** | ✅ 가능 (CC0는 재배포 제한이 없음) |
| ④ 출처 표기 의무 | ❌ 없음 (**단 본 문서에 자발적으로 명시**) |

> ③이 이 프로젝트에서 특히 중요하다. 요강이 **"게임 전체 소스 코드를 동일 저장소에 포함, public 권장"** 을 요구하므로
> 에셋이 공개 저장소에 그대로 올라간다. **"재배포 금지" 조항이 있는 팩은 이 지점에서 위험**하다.
> CC0는 이 문제가 원천적으로 없다. 이것이 A1을 채택한 결정적 이유다.

**표기 의무가 없어도 기재하는 이유:** 약관 제4조 1항이 활용 내역 고지를 의무화했고, 심사자 입장에서 출처가 투명한 편이 신뢰도가 높다.

### A2 라이선스 확인 기록

- GitHub 저장소 `packages/pretendard/dist/LICENSE.txt` 에 **SIL Open Font License 1.1 전문**이 동봉되어 있음 (2026-08-05 원문 확인)
- OFL은 폰트를 다른 소프트웨어와 함께 번들·배포·수정하는 것을 허용하는 라이선스

| 확인 항목 | 결과 |
|---|---|
| ① OFL 1.1 여부 | ✅ |
| ② 상업적 이용 | ✅ 가능 |
| ③ **공개 저장소 포함·재배포** | ✅ 가능 (OFL은 번들 배포 허용) |
| ④ 출처 표기 의무 | ❌ 폰트 자체 재판매만 제한, 게임에 번들하는 데는 표기 의무 없음 (본 문서에 자발적으로 명시) |

### A3 라이선스 확인 기록

- 저장소 `LICENSE.txt` 에 **SIL OFL 1.1 전문**이 동봉되어 있음 (2026-08-06 원문 확인, `Assets/Fonts/neodgm-LICENSE.txt` 로 함께 보관)
- 저작권 표기: *Copyright (c) 2017-2021, Eunbin Jeong (Dalgona.)* · 예약 글꼴 이름 *"Neo둥근모" / "NeoDunggeunmo"*
- 원본은 1990년대 김중태의 공개 도메인 비트맵 글꼴이고, 이를 트루타입으로 변환·확장한 것 (폰트 메타데이터에 명시)

| 확인 항목 | 결과 |
|---|---|
| ① OFL 1.1 여부 | ✅ |
| ② 상업적 이용 | ✅ 가능 |
| ③ **공개 저장소 포함·재배포** | ✅ 가능 |
| ④ 게임 임베딩 | ✅ 제작자가 명시적으로 허용 (*"상업용 또는 비 상업용 앱 및 게임에 글꼴 파일을 임베딩할 수 있습니다"*) |

> ⚠️ **예약 글꼴 이름(RFN).** OFL 은 **글꼴을 수정해 배포할 때** 예약 이름을 못 쓰게 한다.
> 우리는 `neodgm.ttf` 를 **손대지 않고 그대로** 넣었고, TMP 폰트 에셋(`neodgm SDF.asset`)은
> 렌더링용 아틀라스·메트릭이지 글꼴 자체의 개작이 아니다. `A2` 를 다룬 방식과 같다.

---

## 2. 실제 사용 내역 (구현 진행에 따라 갱신)

| 분류 | 사용 파일 | 용도 |
|---|---|---|
| 폰트 | `Assets/Fonts/neodgm.ttf` → `neodgm SDF.asset` | **UI 텍스트 전체**(TMP 기본 폰트). 2026-08-06 교체 |
| 폰트(폴백) | `Assets/Fonts/Pretendard-Regular.otf` → `Pretendard-Regular SDF.asset` | `neodgm` 에 없는 글자만. 실제로 `←`(U+2190)가 두부(□)로 떴다 |
| UI 테마 | `Ui/Theme/Theme Wood/*.png` 42개 → `Assets/Sprite/UI/Theme/` | 패널·버튼·슬롯·탭·슬라이더(9슬라이스) + **버튼 상태 3종**(`_hover`·`_pressed`·`_disabled`) · **탭 선택 상태** · `arrow_left`. 2026-08-06 |
| UI 게이지 | `Ui/Receptacle/*` 5개 → `Assets/Sprite/UI/Bar/` | **전투 체력바**(`LifeBarMiniUnder`/`LifeBarMiniProgress`). 나머지 3종 미사용 |
| 캐릭터 | `Actor/Character/*` (6종 선정) | 플레이어 로스터 |
| 캐릭터 초상 | `Actor/Character/*/Faceset.png` | 로스터 선택 UI |
| 캐릭터 애니메이션 | `SeparateAnim/{Idle,Attack,Dead,Special1,Special2}.png` | 전투 연출 |
| 적 | `Actor/Monster/*` (66종 중 선정) | 1~7라운드 관문 |
| 보스 | `Actor/Boss/*` (20종 중 선정) | 8라운드 최종 관문 |
| 스킬 아이콘 | `Ui/Skill Icon/{Spell,Job & Action,Items & Weapon,Meteo}` | 스킬 12종 + 강화 단계. **메타 재화 아이콘**(`Meta/M-GOLD_재화.png`)도 이 중 `Items & Weapon/Money.png` |
| 재화 아이콘(런 전용) | `Items/Resource/GemPurple.png` → `Assets/Sprite/UI/RunCurrency_재화.png` | 런 전용 재화(`RunState.Currency`) — 메타 재화(금화)와 다른 그림으로 구분. 상점·전투 승리 화면에서 사용. 2026-08-06 |
| 캐릭터 무기 배지 | `Items/Weapons/{Katana,Stick,Sai,Bow,MagicWand,Book}/Sprite.png` → `Actor/Character/*/Weapon.png` | 전투 보드에서 캐릭터 모서리에 붙는 장착 배지(사무라이→Katana·수도승→Stick·적영→Sai·사냥꾼→Bow·주술사→MagicWand·무녀→Book). 손에 합성하는 `SpriteInHand`는 프레임별 정렬 정보가 없어 미사용. 2026-08-06 |
| ~~UI~~ | ~~`Ui/Theme/Theme Wood`, `Ui/Dialog`, `Ui/Input`, `Ui/Font`~~ | **실제로 쓴 것은 위 "UI 테마"·"UI 게이지" 두 줄뿐이다.** `Dialog`·`Input`·`Font` 는 안 씀 |
| 이펙트 | `FX/{Attack,Slash,Magic,Elemental,Projectile,Particle,Smoke}` | 타격·스킬 연출 |
| 효과음 | `Audio/Sounds/{Hit & Impact,Magic & Skill,Whoosh & Slash,Menu,Bonus,Alert}` | 전투·UI 사운드 |
| BGM | `Audio/Musics/*.ogg` (41곡 중 선정) | 메뉴·전투·결과 |

---

## 3. 오픈소스 · 도구

### 3.1 산출물에 포함되는 것 (런타임)

| # | 항목 | 라이선스 | 용도 |
|---|---|---|---|
| O1 | **Newtonsoft.Json for Unity** (`com.unity.nuget.newtonsoft-json`) | MIT | `/data` JSON 로드. **core / sim / unity 3곳이 같은 파서를 쓴다** |
| O2 | Unity UI Toolkit · Unity Test Framework | Unity 내장 | UI(UXML/USS), 테스트 |

### 3.2 빌드 · CI

| # | 항목 | 라이선스 | 용도 |
|---|---|---|---|
| O3 | **GameCI** (`game-ci/unity-actions` 등) | MIT | GitHub Actions에서 Unity WebGL 빌드·배포 |
| O4 | **NUnit 3.14.0** | MIT | 코어 테스트 (`dotnet test`). 버전을 3.x 로 고정한 이유는 아래 |

> ⚠️ **정정 (2026-08-02).** 원래 *"Unity Test Framework 와 `dotnet test` 양쪽에서 같은 테스트 어셈블리를 공유"* 라고 썼는데 **현재 구조로는 성립하지 않는다.**
>
> Unity 는 `Assets/` 아래 또는 **패키지 안**의 asmdef 만 컴파일한다.
> `core.tests/` 는 저장소 루트에 있어 둘 다 아니다 — **Unity 는 이 폴더를 아예 보지 않는다.**
> (확인: Unity 가 생성한 csproj 목록에 `DomoNinja.Core.Tests` 가 없다. `Core` 와 `Unity` 둘뿐이다.)
>
> **NUnit 을 3.x 로 고정한 판단 자체는 유효하다** — UTF 가 NUnit 3 계열이라, 나중에 공유하려 할 때
> 4.x 였으면 Assert 문법 차이로 소스를 못 옮긴다. **선택은 맞았고 서술이 앞서 나갔다.**
>
> → 실제로 공유하려면 테스트를 `core/Tests/` (패키지 안)로 옮기고 `manifest.json` 의 `testables` 에
> `com.domoninja.core` 를 등록해야 한다. **Unity 테스트 통합은 P5 작업이므로 그때 처리한다** (`19` §6.6).
> 지금은 **`dotnet test` 단독**이고, 결정론 회귀는 CI 에서 그것으로 검증된다.

### 3.3 개발 시점 도구 (산출물에 포함되지 않음)

| # | 항목 | 라이선스 | 용도 |
|---|---|---|---|
| # | 항목 | 버전 | 라이선스 | 용도 |
|---|---|---|---|---|
| O5 | **Unity MCP** (`com.unity.ai.assistant`) | 2.17.0-pre.1 | Unity 내장 | AI가 Unity 씬·콘솔에 직접 접근. **에러 피드백 루프를 닫는 용도** |
| O6 | **Game-UI-Reference-Cli** (SillyToolValley) | — | MIT | UI 레퍼런스 로컬 인덱싱·대조표 생성 (조건부, 미설치) |
| O7 | **Optuna** (optuna/optuna) | **4.9.0** | MIT | 밸런스 외부 루프 — ASHA 가지치기 + CMA-ES/TPE 샘플러. **ask-and-tell로 시뮬 CLI를 구동** |
| O8 | **SALib** (SALib/SALib) | **1.5.2** | MIT | Morris 민감도 스크리닝 — 어느 파라미터를 탐색할지 측정으로 결정 |
| O9 | **cmaes** (CyberAgentAILab) | **0.13.0** | MIT | O7의 CMA-ES 백엔드. **CMAwM이 정수·이산 파라미터를 처리** |
| O10 | **NumPy** | **2.5.1** | BSD-3-Clause | `M4b`(비추이성) 계산에 직접 쓴다 |

> ★ **O5 정정 (2026-08-02).** `13` §4.5.1에서 채택했던 서드파티 **IvanMurzak/Unity-MCP 는 쓰지 않는다.**
> **Unity 6에 공식 MCP가 내장**되어 있었다. 서드파티 의존이 하나 사라졌다.
> ⚠️ 같은 패키지의 `Unity_AssetGeneration_GenerateAsset` 은 **사용하지 않는다** (`D-19` AI 생성 에셋 배제).

> **O7~O10은 `/tune` (Python)에서만 쓰인다.** 게임 빌드에 들어가지 않고, **`/core`·`/sim`은 Python에 의존하지 않는다**(CI로 강제).
> 근거·기각 후보는 `17_최적화_방법론.md` §8.

**설치 완료 (2026-08-02, D+2).** `tune/.venv` · Python 3.12.10 · 고정 파일 2개:

| 파일 | 내용 |
|---|---|
| `tune/requirements.txt` | 직접 의존 4개를 `==` 로 고정 + 채택 사유 주석 |
| `tune/requirements.lock` | `pip freeze` 전체 29개. **전이 의존성까지 재현 가능** |

동작 검증(설치만으로는 채택 근거가 증명되지 않아 실제로 돌려봤다):

| 확인 | 결과 |
|---|---|
| **Optuna ask-and-tell** — O7 채택의 결정적 근거 | ✅ `study.ask()` / `tell()` 왕복 성공 |
| SALib Morris 샘플링 | ✅ `(12, 2)` 설계 행렬 생성 |
| `CmaEsSampler` 로드 | ✅ |

### 3.3.1 전이 의존성 라이선스 감사 (§갱신규칙 "GPL 계열 배제" 확인)

설치된 **29개 전량**을 확인했다. **GPL·AGPL·LGPL·CC-BY-NC 없음.**

| 라이선스 | 패키지 |
|---|---|
| MIT | optuna, salib, cmaes, sqlalchemy, alembic, Mako, PyYAML, colorlog, fonttools, six, pyparsing, greenlet, pip |
| BSD 계열 | numpy, scipy, pandas, contourpy, cycler, kiwisolver, MarkupSafe, dill, multiprocess, colorama, python-dateutil |
| 기타 허용 | matplotlib(PSF), pillow(MIT-CMU), typing_extensions(PSF-2.0), packaging(Apache-2.0 OR BSD-2), tzdata(Apache-2.0) |
| ⚠️ **tqdm** | **MPL-2.0 AND MIT** — 아래 참조 |

> ⚠️ **tqdm 하나만 비허용(weak copyleft)이다.** MPL-2.0은 **파일 단위** copyleft라
> *"MPL 파일을 수정해서 배포할 때"* 만 소스 공개 의무가 생긴다.
> **우리는 (1) 수정하지 않고 (2) `/tune` 전용이라 게임 빌드에 포함되지 않으며 (3) 애초에 직접 부르지도 않는다**(optuna의 진행 표시용 전이 의존).
> → **문제없다.** 다만 GPL이 아니라는 이유로 넘기지 않고 여기 명시해둔다 — 감사에서 걸리는 건 대개 이런 항목이다.

### 3.4 자체 구현 (외부 코드 차용 없음)

전투 코어 · 헤드리스 시뮬레이터 · **밸런스 목적함수** · **지배 빌드 지표(`M4a`/`M4b`)** · 커밋 규약 CI 검증 — **전부 직접 작성한다.**

> 근거: 약관 **제4조 2항** — "출품작은 참가자 **본인의 순수 창작물**이어야 하며…"
> 오픈소스 오토배틀러 코드 베이스가 다수 존재하지만 **차용하지 않는다** (`13_외부도구_조사.md` §2.3).

> ⚠️ **§3.3의 O7~O9와 경계가 헷갈릴 수 있어 명시한다.**
> **탐색 알고리즘은 가져다 쓰고(O7~O9), 무엇을 최적화할지는 직접 정의한다(§3.4).**
> 표준 수치 도구를 쓰는 것은 컴파일러를 쓰는 것과 같고, 약관 제4조 2항이 겨냥한 "게임 코드 베이스 차용"과 성격이 다르다.
> **밸런스 최적화기를 통째로 직접 짜겠다던 초기 계획은 `17` §8에서 철회했다** — 재발명에 쓸 시간을 목적함수 설계로 옮겼다.

### 3.5 조사했으나 채택하지 않은 것 (기술 문서에 기각 사유 기재)

| 항목 | 기각 사유 |
|---|---|
| **BMAD-METHOD / GitHub Spec Kit** 등 SDD 프레임워크 | 볼륨 불일치 + **채택하면 "설계"가 아니라 "활용"으로 읽힌다** |
| **FixedMathSharp** (결정론 고정소수점) | 설계상 부동소수점을 쓸 자리가 없다. **v0.5에서 이동이 도입됐지만(`A-1`) 좌표가 정수·거리가 정수 제곱거리라 판단은 유지된다** |
| **OpenSpiel** (α-Rank) | Windows 네이티브 미지원 + C++ 빌드로 무겁다 + **PvE라 빌드 간 승률 행렬이 없어 α-Rank가 애초에 안 맞는다.** 개념만 차용 |
| **CMAES.NET / Math.NET Numerics** | 전자는 2020년 이후 방치(★11), 후자는 국소·미분 기반. **C#에 쓸 만한 블랙박스 탐색기가 없다** → 외부 루프를 Python으로 분리 |
| **만족도 함수 라이브러리** (R `desirability` 등) | 30줄이면 되고, **목적함수는 디렉팅 층의 산출물이라 우리 것이어야 한다** |
| **husky + commitlint** | `.git/hooks`는 버전 관리 대상이 아니라 심사자에게 안 보인다 → **CI 검증으로 대체** |
| **System.Text.Json** | 성능 우위는 있으나 **우리는 시작 시 1회 로드**라 무의미. Unity WebGL/IL2CPP 통합 리스크가 더 크다 |

---

## 4. 참고 문헌 (코드 차용 없음, 설계 근거로만 인용)

| # | 문헌 | 인용 위치 |
|---|---|---|
| R1 | RuleSmith: Multi-Agent LLMs for Automated Game Balancing (arXiv 2602.06232, 2026) | 밸런스 루프의 탐색/디렉팅 2층 구조 설계 근거. 적응적 샘플링 차용 |
| R2 | α-Rank: Multi-Agent Evaluation by Evolution (arXiv 1903.01373 / Sci. Rep. 2019) | `M4` 재정의. **코드는 쓰지 않고 「평균 승률은 상대 분포에 오염된다」는 문제의식만 차용** |
| R3 | Derringer & Suich, 만족도 함수 (다중 응답 최적화) | 목적함수 — `M1`~`M7` 구간 목표를 기하평균 단일 점수로 |
| R4 | Successive Halving / Hyperband (arXiv 2007.15745 등) | 시드 예산 배분. Optuna의 ASHA 가지치기로 구현 |
| R5 | Morris, Elementary Effects (SALib 구현) | 탐색 대상 파라미터 선정 |
| R6 | Common Random Numbers (분산 감소 기법) | 파라미터 세트 비교 시 시드 공유. `05` §2.4 결정론의 두 번째 용도 |

> **R2는 인용 방식에 주의한다.** α-Rank를 "썼다"고 쓰면 사실과 다르다.
> **PvE라 적용 조건이 안 맞는다는 것까지 포함해** 기술 문서에 서술한다 — 한계를 밝히는 쪽이 신뢰도가 높다.

---

## 5. AI 도구 사용 내역 (약관 제4조 1항 의무 고지)

| 도구 | 용도 |
|---|---|
| Claude (Claude Code) | 기획 문서 작성, 설계 검토, 코어·시뮬레이터·UI 코드 생성, 밸런스 리포트 해석 |

> 상세한 활용 방식·프롬프트·사람 개입 지점은 **AI 활용 기술 문서** 본문에 기술한다.
> ⚠️ **AI 생성 이미지·사운드는 사용하지 않는다** (`D-19`). A1 팩만으로 충분하고, 스타일 일관성과 라이선스 단순성이 더 중요하다고 판단.

---

## 갱신 규칙

- **새 에셋을 쓰는 즉시** §2에 행을 추가한다
- 새 소스를 추가하면 §1에 ①~④ 4개 항목을 모두 확인해 기록한다
- **CC-BY-NC · GPL 계열은 채택하지 않는다** — 약관 제7조(수상작 사업화 우선협상권)와 부딪힐 소지
