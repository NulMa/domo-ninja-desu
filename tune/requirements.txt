# /tune — 밸런스 최적화 외부 루프 (D-49-a)
#
# 여기 있는 것은 「통계·탐색」뿐이다. 게임 로직은 한 줄도 없다.
# /sim (C#) 이 params.json -> metrics.json CLI 이므로, 이 층은 그 바이너리를 호출만 한다.
#   → 결정론은 C#, 통계는 Python. 언어를 나눈 게 아니라 역할을 나눈 것이다. (17 §8)
#
# 설치:
#   py -m venv .venv
#   .venv/Scripts/python.exe -m pip install -r requirements.txt
#
# 정확한 재현이 필요하면 requirements.lock 을 쓴다 (전이 의존성까지 고정).
#
# 채택 근거는 docs/17_최적화_방법론.md §8. 라이선스는 docs/14_에셋_라이선스.md.

optuna==4.9.0     # MIT. ask-and-tell + ASHA 가지치기 + TPE/CMA-ES 샘플러
salib==1.5.2      # MIT. Morris 스크리닝 (민감도 분석)
cmaes==0.13.0     # MIT. Optuna 백엔드. CMAwM 이 정수·이산 파라미터를 처리한다
numpy==2.5.1      # BSD-3. M4b(비추이성) 계산에 직접 쓴다

# 아래는 위 4개가 끌고 오는 전이 의존성이라 여기 적지 않는다 (버전은 requirements.lock).
#   scipy pandas matplotlib  <- salib
#   sqlalchemy alembic       <- optuna (study 저장소)
