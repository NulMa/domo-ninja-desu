<!--
  IL2CPP 코드 스트리핑 보존 목록.

  ★ 이 파일이 없으면 `ManagedStrippingLevel` 을 올리는 순간 게임이 **런타임에 조용히 깨진다.**
    스트리퍼는 정적 참조를 따라가는데, Newtonsoft.Json 은 리플렉션으로 타입을 만든다 —
    코드 어디에도 "이 타입을 쓴다"는 참조가 없으므로 **쓰이는데도 잘려나간다.**

  ★ 그리고 그 실패는 **빌드에서 안 드러난다.** 컴파일도 되고 빌드도 성공하고,
    브라우저에서 데이터를 읽는 순간에야 터진다. `19` §6.1a 가
    *"스트리핑이 뭘 잘라냈는지는 브라우저에서만 드러난다"* 고 적어둔 게 이 뜻이다.

  보존 범위를 최소로 잡았다 — 전부 보존하면 스트리핑을 켠 의미가 없다.
-->
<linker>

  <!--
    JSON 역직렬화 대상. `/data` 5개 파일이 이 어셈블리의 타입으로 올라온다.
    Newtonsoft 가 리플렉션으로 생성자와 프로퍼티를 찾으므로 멤버까지 남긴다.
  -->
  <assembly fullname="DomoNinja.Core" preserve="all" />

  <!--
    파서 본체. `JsonConvert` 내부가 제네릭 + 리플렉션이라 부분 보존이 위험하다.
    ⚠️ 여기가 이 파일에서 가장 큰 덩어리다. 크기를 더 줄여야 하면
       `preserve="all"` 대신 실제 쓰는 타입만 열거하는 쪽으로 좁힌다 —
       다만 **좁힐 때마다 브라우저에서 다시 확인해야 한다.**
  -->
  <assembly fullname="Newtonsoft.Json" preserve="all" />

</linker>
